# Brain storm folder

Helps to make relation between thoughts and prioritize them in tasks

```mermaid 
mindmap
  root((brainstorm))
    api-gateway
        apollo federation graphq
    ddd princeple
        bounded context
        user entity
        event entity
            point lat lon
            time interval
    auth
        key-clock
        other nodejs auth servers
        users entity
```